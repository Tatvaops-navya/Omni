# Admin UI — loaded from repo root .env via Vite (envDir: ..)

# Tatva — /tatva-api proxies to https://api.withtatva.ai
VITE_TATVA_API_BASE_URL=/tatva-api

# Vite dev proxy for /admin only (login, enquiries, CRM) — not exposed to browser
OMNICHANNEL_API_URL=https://omni-1-iltl.onrender.com
